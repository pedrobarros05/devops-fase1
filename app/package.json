{
  "name": "devops-fase1-app",
  "version": "1.0.0",
  "description": "Aplicação web — DevOps na Prática Fase 1",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "build": "mkdir -p dist && cp -r src/* dist/",
    "test": "jest --coverage",
    "lint": "eslint src/ tests/"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  },
  "jest": {
    "testMatch": ["**/tests/**/*.test.js"]
  }
}
